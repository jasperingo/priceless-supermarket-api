import { PhotoValidationPipe } from './photo-validation.pipe';

describe('PhotoValidationPipe', () => {
  it('should be defined', () => {
    expect(new PhotoValidationPipe()).toBeDefined();
  });
});
