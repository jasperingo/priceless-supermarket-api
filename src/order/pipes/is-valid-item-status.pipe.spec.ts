import { IsValidItemStatusPipe } from './is-valid-item-status.pipe';

describe('IsValidItemStatusPipe', () => {
  it('should be defined', () => {
    expect(new IsValidItemStatusPipe()).toBeDefined();
  });
});
