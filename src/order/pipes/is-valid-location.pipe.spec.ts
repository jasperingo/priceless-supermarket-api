import { IsValidLocationPipe } from './is-valid-location.pipe';

describe('IsNotInvalidLocationPipe', () => {
  it('should be defined', () => {
    expect(new IsValidLocationPipe()).toBeDefined();
  });
});
