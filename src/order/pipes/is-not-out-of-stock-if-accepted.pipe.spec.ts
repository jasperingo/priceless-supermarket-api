import { IsNotOutOfStockIfAcceptedPipe } from './is-not-out-of-stock-if-accepted.pipe';

describe('IsNotOutOfStockIfAcceptedPipe', () => {
  it('should be defined', () => {
    expect(new IsNotOutOfStockIfAcceptedPipe()).toBeDefined();
  });
});
