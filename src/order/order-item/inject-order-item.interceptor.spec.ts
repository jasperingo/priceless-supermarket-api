import { InjectOrderItemInterceptor } from './inject-order-item.interceptor';

describe('InjectOrderItemInterceptor', () => {
  it('should be defined', () => {
    expect(new InjectOrderItemInterceptor()).toBeDefined();
  });
});
