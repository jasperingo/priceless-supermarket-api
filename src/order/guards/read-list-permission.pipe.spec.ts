import { ReadListPermissionPipe } from './read-list-permission.pipe';

describe('ReadListPermissionPipe', () => {
  it('should be defined', () => {
    expect(new ReadListPermissionPipe()).toBeDefined();
  });
});
