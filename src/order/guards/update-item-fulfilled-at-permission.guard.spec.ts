import { UpdateItemFulfilledAtPermissionGuard } from './update-item-fulfilled-at-permission.guard';

describe('UpdateItemFulfilledAtPermissionGuard', () => {
  it('should be defined', () => {
    expect(new UpdateItemFulfilledAtPermissionGuard()).toBeDefined();
  });
});
