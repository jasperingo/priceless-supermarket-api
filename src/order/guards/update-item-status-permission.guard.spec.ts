import { UpdateItemStatusPermissionGuard } from './update-item-status-permission.guard';

describe('UpdateItemStatusPermissionGuard', () => {
  it('should be defined', () => {
    expect(new UpdateItemStatusPermissionGuard()).toBeDefined();
  });
});
