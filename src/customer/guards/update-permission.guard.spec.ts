import { UpdatePermissionGuard } from './update-permission.guard';

describe('WritePermissionGuard', () => {
  it('should be defined', () => {
    expect(new UpdatePermissionGuard()).toBeDefined();
  });
});
