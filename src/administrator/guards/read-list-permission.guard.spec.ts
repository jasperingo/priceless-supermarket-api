import { ReadListPermissionGuard } from './read-list-permission.guard';

describe('ReadListPermissionGuard', () => {
  it('should be defined', () => {
    expect(new ReadListPermissionGuard()).toBeDefined();
  });
});
